services:
  tarot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tarot-app
    ports:
      - "4025:4025"
    environment:
      - NODE_ENV=production
      - PORT=4025
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
