services:
  tarot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tarot-app
    ports:
      - "4025:4025"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=4025
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
